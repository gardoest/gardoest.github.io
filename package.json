{
  "name": "dle-score-tracker",
  "version": "1.0.0",
  "description": "Daily Leaderboard with Claude AI parsing",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.0"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  }
}
